# ADR-0001：薄运行时

上下文：UFO/Cua/Rivet 等已有相关能力，重做完整平台成本与重复度高，见调研 S01/S03/S04。

决定：模型/Agent 留在上层；本项目提供任务契约、结果证据与执行恢复。初始单机，不建新内核、浏览器或模型调度器。

后果：早期可能只是 CLI/MCP 服务，不具备 OS 外观。它必须证明跨任务复用和 B→C 增量；若只需某个适配器，则接受收敛。

重新考虑条件：两个以上任务族已通过实测，现有后端成为可复现瓶颈，且扩展成本有明确预算。
