# ADR-0003：本地持久化与诚实恢复

决定：v0 用 SQLite 存任务状态、操作意图与证据索引，本地 blob 存不可变产物。状态变化与事件在一笔数据库事务内写入，文件 staging 另行对账。

不承诺：数据库事务不覆盖浏览器、Office、文件系统与网络服务的全部效果。无法判断写入是否发生时标 unknown_effect，阻断自动重试。

后果：单机并发能力有限；需要迁移、备份与清理策略。但能减少基础设施与排错成本，不提前部署数据库集群或图数据库。

重新考虑条件：真实负载测得单机瓶颈，或多用户权限/持久服务需求成为已验收范围。
