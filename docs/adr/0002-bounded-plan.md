# ADR-0002：受限计划而不是任意执行

上下文：程序化工具组合可降低模型往返（研究 S12），但任意代码加宿主权限会扩大风险。

决定：v0 只允许注册动作的有限 JSON DAG；批处理由受信适配器实现。所有参数解析与资源效果都经过 broker，无 eval、任意 Shell 或动态安装。

后果：表达能力低于通用代码模式；未知动作会被拒绝，而不是让 Agent 临时绕路。性能基线 B 可以使用受审查批处理脚本，权限仍须对齐。

重新考虑条件：有无法用合理适配器表达的高价值场景，且隔离运行库/VM 边界通过安全实验。即使引入代码模式，也不允许绕过最终效果检查。
