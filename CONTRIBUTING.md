# Contributing

本项目尚处策划阶段。贡献以可验收的任务为单位，见 [清单](planning/BACKLOG.md)。

提交代码前先复现需求，写出预期输入、输出与失败行为，再实现最小改动。PR 应引用任务 ID，附上实际运行的测试与环境；未测试的 Windows、Office、Blender 路径必须注明。不要把模拟测试写成真实软件兼容性证明。

禁止提交密钥、Cookie、会话、个人文件、受限商业资料与未经许可的素材。测试数据必须合成。外部代码引入前记录版本、许可、运行时依赖和安全边界；引用来源并不允许任意复制源代码。

性能改动应对比同一任务与基线，包含失败和重试成本。无法改善结果时保留负面结论，不选择性删除失败样本。

安全问题不要附带可用凭据发布到公开 Issue。远端仓库建立后优先启用 GitHub 私密漏洞报告；在该渠道尚未启用前，不声称存在受理邮箱或响应 SLA。
