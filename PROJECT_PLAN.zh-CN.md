# AgentOS Runtime：立项与实施总策划

策划版本：0.1；日期：2026-09-11。下文保留初始设计；最新代码状态见 [STATUS](STATUS.md)，当前仅有离线合成任务技术验证。

## 一、项目到底做什么

为现有 Agent 增加一个本地执行层：模型一次提交明确的工作计划，电脑执行能确定的步骤，保留中间成果，在需要判断或授权时才把问题交回模型或人。

首版不是新桌面，也不是自动点击工具大全。目标只有四个：减少模型往返；减少中间数据进出上下文；减少串行等待；减少失败重跑。结果质量和权限边界不能作为换速度的代价。

## 二、调研改变了哪些判断

同类项目已经覆盖大量最初设想。微软 UFO 有 Windows 原生接口、GUI/API 混合执行和批量动作；Cua 提供电脑驱动与隔离环境；Rivet agentOS 把进程、文件与权限抽象为可嵌入运行库；Vercel agent-browser 已有批处理 CLI。来源与限制见 [调研矩阵](docs/research/LANDSCAPE.md) 的 S01、S03、S04、S07。

因此不再把“有 MCP、有批处理、有记忆、有调度”作为原创成果。需要验证的是：这些能力在同一套任务契约下组合后，是否让真实任务交付更快、恢复更省事，同时不增加过多维护成本。

第一项工作不是搭完整平台，而是把现成工具跑成一个强基线。已经有合适项目能完成目标时，允许选择集成或贡献上游，而不是为了拥有仓库重新实现。

## 三、我们要服务谁

首期使用者是愿意委托 Agent 做重复电脑工作的个人。使用者不需要理解线程、数据库和虚拟机，只需要选输入、说明目标、查看变化和处理授权。

开发者面对的接口则必须严格：输入结构、权限、副作用、恢复规则和验收结果都能被程序读取。不能依靠用户始终盯着日志弥补系统缺陷。

部署假设是单人、单机、本地优先。桌面扩展暂按 Windows-first 规划，但实际操作系统、应用版本和权限尚未采集；跨平台文件与模拟网页先行。

## 四、首条可验收任务

使用完全合成的 100 份 CSV，每份含 item_id、category、quantity、unit_price、currency。规则固定：金额使用十进制定点规则；不跨币种直接相加；缺失/重复/非法值必须进入异常清单。

执行流程：导入只读输入 → 校验 → 按币种与类别汇总 → 生成 summary.json、summary.csv、errors.json → 将汇总填入本地模拟网页草稿 → 导出草稿和验证报告。

草稿站点由测试系统提供，不连互联网，不使用真实账号，不点击外部发送。CSV 只是计算用的合成任务载体，不涉及任何人的真实收入、资金或业务数据。

验收检查包括金额、行数、币种、异常计数、草稿字段、输入哈希保持不变，以及每份产物对应的输入版本。操作完成但数字不符，仍是失败。

这个任务有意避开 Office 授权、复杂布局和桌面会话问题，先测执行层的收益。它不能证明任意办公或创作任务都能提效。

## 五、首版范围与明确不做

| 首版要做 | 暂时不做 |
|---|---|
| 有类型的动作契约与有限步骤 DAG | 通用自然语言操作系统内核 |
| 注册过的批量文件/数据/浏览器动作 | 任意 Shell、Python 或 JavaScript 在宿主执行 |
| 产物引用、版本与依赖 | 全电脑实时语义图与知识图谱数据库 |
| SQLite 任务日志、崩溃恢复 | 任意应用的通用回滚、分布式事务 |
| 每个动作执行前的权限校验 | 仅靠提示词的“权限控制” |
| 专用测试浏览器与本地模拟网页 | 默认接管个人浏览器登录态 |
| CLI + MCP 接入现有 Agent | 重做聊天界面、多 Agent 社交平台 |
| A/B/C 对照与故障注入 | 只录成功演示、宣传未测倍率 |

## 六、总体架构

```text
现有 Agent / 人类任务入口
           |
       MCP 或 CLI
           |
  任务契约校验 / 计划哈希
           |
  策略代理：资源、操作、额度、授权
           |
  有界执行器：依赖、锁、超时、取消
      /          |          \
 文件/数据适配器  浏览器适配器  将来的桌面适配器
      \          |          /
   产物库 + SQLite 状态/事件 + 独立验证器
           |
  摘要、异常、证据、需要人工决策的问题
```

模型不直接拥有“绕过策略的后门”。所有受本项目控制的调用必须经过执行代理。已有 Agent 自带的 Shell 等外部通道不在本项目的约束内，因此不能宣称整个电脑已被隔离。

技术默认：Python、Pydantic、SQLite、本地文件产物库；浏览器从 Playwright 或 agent-browser 二选一做选型实验；MCP 用官方 SDK。版本在编码前固定，不在策划阶段凭记忆承诺“最新版本”。

对动态 Agent 编排可复用 LangGraph 等上层框架；本项目只管理确定动作及其外部效果，不自研一整套模型框架。Rivet 与 Cua 是需做兼容性实验的候选，不同时强制引入多个复杂底座。

## 七、真实效率如何测量

A：现有 Agent 按步骤调用工具。B：同样的适配器与权限，但允许成熟的批处理/代码编排。C：B 加上本项目的任务状态、产物引用、恢复与策略。

A→B 证明批处理和接口的作用；B→C 才证明运行时的增量。不得用“逐像素点击的弱基线”给运行时制造优势。

必须记录所有尝试，包括超时、人工接管和失败：最终合格率、端到端时间、模型请求数、输入/输出 Token、人工占用时间、重复执行步骤数、峰值资源、总费用、维护时间。

立项门槛不是实测结果：C 相对 B，在任务质量不下降的前提下，目标为至少两个不同任务族中的人工介入或失败恢复时间降低 30%；干净无故障任务的中位耗时回退不超过 10%。阈值需在正式实验前锁定，不能看到结果再修改。

试验样本小的时候只报告探索性结果。没有足够证据就写“尚未证明”，而不是把偶然成功写成性能结论。完整设计见 [BENCHMARKS](docs/BENCHMARKS.md)。

## 八、阶段推进，不按日期硬压交付

| 阶段 | 产物 | 过关条件 |
|---|---|---|
| M0 复用与基线 | 固定任务、强基线、版本/许可清单 | 能解释现成工具缺哪一块，否则停止自研 |
| M1 最小执行闭环 | 类型契约、只读输入、批量数据处理、结果证据 | 无模型也可重放受控计划，失败可定位 |
| M2 可靠执行 | 持久日志、产物引用、重启恢复、权限拒绝 | 故障注入不改原稿，不盲目重放未知副作用 |
| M3 Agent 接入与第二任务族 | MCP、浏览器草稿、独立批量文件任务 | 两类任务复用运行时，非复制两套脚本 |
| M4 测量与个人预览 | A/B/C 报告、诊断、最小交互、安装验证 | 达到事先定义的投资门槛再发布预览 |
| M5 条件扩展 | Windows/Blender/Office 等单项试点 | 每新增适配器先给出任务量、收益与维护预算 |

完整任务和依赖见 [BACKLOG](planning/BACKLOG.md)。任务数不是承诺工期；基线结果、实际电脑兼容性与安全边界会决定哪些任务继续。

## 九、把高风险问题提前处理

1. **执行已发生、记录未落盘。** 这时不能直接重试；用幂等标识、结果核对或转人工。任务检查点不是撤销外部行为的按钮。
2. **页面或文件诱导越权。** 外部内容只作数据，不改变权限。审批由人类控制面发出，不向 Agent 暴露自我批准接口。
3. **批量计划过期。** 对每次动作重新核对对象版本和授权；中途变化就停止或缩小重做范围。
4. **强基线已经足够。** 先衡量 B，允许结论是只做适配器，不继续做平台。
5. **第三方更新或许可不清。** 固定版本、记录来源；AIOS 当前根 LICENSE 内容异常需澄清，Bytebot 已归档只作参考。详见调研 S02、S08。
6. **用户不能调试。** 每个错误都要解释影响范围、保留成果与下一步；不得把“看堆栈自行处理”作为正常体验。

## 十、之后进入编码时，从什么开始

首轮只做：工程骨架、合成测试任务、动作契约、只读输入与产物写入、串行执行器、事件记录、独立验证。先不接真实账号，不付费调用模型，不部署后台开机常驻。

结束时应交付一个可测试的闭环，而不是大量没有联通的模块。随后才加重启恢复、有限并行、MCP 与浏览器适配。

## 十一、什么情况下停止扩大项目

若同等权限下 B 已经满足任务；若 C 增加复杂性但未减少人力；若两类任务无法复用契约；若只能在放松质量/安全时显得更快——应缩小范围、改为上游扩展，或结束通用运行时方向。

成功不是拥有一个叫 OS 的仓库。成功是：同样的工作少等待、少交接、少返工，并能被重复验证。

## 十二、本轮交付的真实状态

已完成策划包、同类调研、任务依赖与首轮编码指令。没有运行时实现，没有性能测量，也没有已创建的 GitHub 仓库或 Issues。X 搜索未取得可核验正文，不对 X 社区意见作推断。

## 实施状态补记（v0.0.2）

上述为立项时策划。最新切片、实际测试、MCP/浏览器等未完成项以 [STATUS.md](STATUS.md) 和 [第二轮报告](reports/IMPLEMENTATION_R2.md) 为准。默认云端继续开发，不要求非技术用户先开本地编码会话；平台 go/no-go 仍未决。
